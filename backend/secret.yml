apiVersion: v1
kind: Secret
metadata:
  name: db-credentials       # this matches the `name:` under secretKeyRef in your Deployment
  # namespace: your-namespace  # optional: specify if not in default namespace
type: Opaque
data:
  password: R2FuZXNoJDk1Nzk=   # base64-encoded value of your DB password (e.g., "password123")
